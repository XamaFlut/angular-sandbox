import { PasswordPeakDirective } from './password-peak.directive';

describe('PasswordPeakDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordPeakDirective();
    expect(directive).toBeTruthy();
  });
});
